{
  "name": "@ghost-compute/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-specifier-resolution=node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5",
    "undici": "^6.19.2",
    "@ghost-compute/shared": "workspace:*",
    "@ghost-compute/crypto": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "tsx": "^4.15.7",
    "@types/node": "^20.14.9"
  }
}
